# Fixtures

This directory contains test data and custom Playwright fixtures.

- **JSON files**: Store static data for tests (e.g., `users.json`, `products.json`).
- **Custom fixtures**: Extend Playwright's built-in fixtures for test setup (e.g., automatically logging in a user).
