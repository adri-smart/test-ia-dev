# Helpers

This directory contains utility functions and helper classes that can be used across the test suite.

Examples:
- **API Helpers**: Functions to interact with the application's API for test setup and teardown (e.g., seeding a database, creating a test user).
- **Auth Helpers**: Functions to handle complex authentication flows.
- **Data Generators**: Functions to create dynamic test data.
